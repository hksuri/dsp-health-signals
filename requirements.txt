# Core DSP / plotting stack (pinned to a tested, working set).
# Use `>=x,<next-major` ranges so the repo keeps installing cleanly over time
# while staying on the same major versions these scripts were validated against.
numpy>=2.4,<3
scipy>=1.17,<2
matplotlib>=3.10,<4

# Needed for the MIT-BIH validation that downloads PhysioNet record 100
# (filters-and-detection/exercise2 and ppg/exercise3). Both scripts degrade
# gracefully if it is missing.
wfdb>=4.3,<5

# Only needed for the CWT scaleogram figure in exercise 4. The script degrades
# gracefully (skips that figure) if it is missing.
PyWavelets>=1.6,<2
