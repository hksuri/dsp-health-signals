# ── Python ────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
dist/
*.egg-info/
.eggs/

# ── Virtual environments ──────────────────────────────────────
.venv/
venv/
env/
ENV/

# ── Tooling / caches ──────────────────────────────────────────
.pytest_cache/
.mypy_cache/
.ruff_cache/
.ipynb_checkpoints/

# ── MIT-BIH / PhysioNet download cache ────────────────────────
# wfdb caches downloaded records locally; don't commit clinical data dumps.
mitdb/
*.dat
*.hea
*.atr
mit-bih*/

# ── OS / editor cruft ─────────────────────────────────────────
.DS_Store
Thumbs.db
.vscode/
.idea/

# ── Project 1 (AFib) downloaded datasets + follow-along figures ─
# Raw PhysioNet dumps (afdb signals, Challenge-2017 training set) and the
# scratch figures used only to *follow along* — neither belongs in the repo.
project1-afib-detection/data/
project1-afib-detection/figures/followalong/

# ── Notes ─────────────────────────────────────────────────────
# Figures in figures/ ARE committed on purpose — the README embeds them.
